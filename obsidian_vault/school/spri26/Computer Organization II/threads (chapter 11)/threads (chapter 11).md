
process: program, memory (text data bss heap stack), execution
	stack directly linked to execution
	function call frame, on the stack

CPU is the execution engine

early computers with one CPU
	shared computing, multiple processes on one CPU
		round robin (quantum)
mid 1970, started doing microprocessors to have smaller less powerful CPU
parallel computing
	multiple small CPU, and one big memory

concurrency
	multiple execution points inside one process

for each thread, they need their own stack
	each stack is stored in the heap


[[definitions, concurrency, parallel and distributed]]
