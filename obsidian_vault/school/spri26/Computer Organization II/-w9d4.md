a5 and a6
	joining threads in the main loop
	struct with thread id and is it done
	then have an array of the struct in main
	-
	thread then sets done before calling pthread_exit
	-
	main then checks if any are done already and joins,
		0 is running
		1 is done
		2 is joined

semaphores
	serial devices
	binary semaphore is a mutex

a6 specific stuff
	computation speed up
	start with mm.c and modify to divide work between threads
	-
	not worried much about thread sync, as you can just make a number of threads needed. if final matrix is a 5x5, just make 25 threads
	reading is shared but you dont need sync for that
	-
	to then have variable num of threads (from user not by output needed)
	say 6 threads and 25 elements of the matrix needing to be computed
	elements / threads rounded to int gets
	elements % threads
	first or last thread can just be given the remainder
	-
	for matrix squaring
	(M^2)\^2\^2 etc
	M^2 must be computed first, then wait before doing the next \^2 
	barrier

matmul
add a threads parameter
change to create thread and join threads
actual calculations happen in thread body

matsquare
currently calls matmul

create a function to calculate a subsection of the matrix multiplication
matmul -> thread body -> helper func
matmul tells thread where to start in the final matrix, and how many elements to calculate

remember to handle matrix looping back on itself
do not do a double loop, just single
loop loops over the number of elements rather than ix jx
	loop updates ix jx as it does (move down a row (ix) when jx at max size)

final report
	htop