
[[File IO]]
[[File Sharing]]

read the first argument in argv to check for builtin

builtin exit() might need to be bi_exit(), bi_cd()
unique names are needed

echo \$$ to check pid value

cd or exit return an error if given too many arguments

remember exit needs to take a argument (int) which is the error code
exit 0 is fine
exit 0 or exit 1 is fine
exit joe should error
exit 1 2 should error
echo $? to check exit code afterwards
maybe have bi_exit(n) call exit(n)