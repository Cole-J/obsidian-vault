
expand function for assignment 2
	want to be able to input "echo my home is ${HOME}."
	-
	instead of outputting "my home is ${HOME}." it should output something like "my home is /home/phil"
	-
	what is happening
	hand expand the newline, and the num of characters in the newline
		epand walks the line checking for ${, check for }, then look for the def of the thing in between {}
		can make a pointer start after the { and make the } a \0
		put that pointer into getenv
		-
		if you get a null ptr, remove the ${} and whats between it
		ex:
			'${JUNK}' = ''
		-
		\$$, replace with the pid of your process (can confirm with the command 'ps' when running the ush)
	-
	LINELENGTH is 1024, use the constant for the number of chars in the newline
	-
	hand arg parse the newline
	before the fork, check for built in commands (stored in argv\[0]) like cd (check with pwd)
	exit command
	envset
	could do
		try-builtin(argv\[0])
		else fork
		-
		or
		-
		if is builtin(argv\[0])
			do builtin(argv)
	will loose points if you are comparing argv\[0] to "cd" ext in ush.c, have a call to check if it is a builtin in builtin.c first.
	all the builtin code should be in its own c file, except for its function call in ush.c

should be able to put into ush command line
	envset X 0123456789
	echo ${X} <--- should be equal to 0123456789
	envset Y \${X}\${X}\${X}\${X}\${X}\${X}\${X}\${X}\${X}\${X}\${X}\${X}\${X}
	envset Z \${Y}\${Y}\${Y}\${Y}\${Y}\${Y}\${Y}\${Y}\${Y}\${Y}\${Y}
		the above should throw error 'ush: expansion too long' and then go back to shell

things can be downloaded by typing the file path into the class website path

[[environment variables]]


remember memory (from bottom to top)
	(inaccessible)
	test
	data
	block storage space(BSS)
	heap
	stack (grows towards the heap)
	environ (but is later moved to the heap)
	