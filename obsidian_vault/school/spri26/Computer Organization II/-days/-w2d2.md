return null or 0 from argparse on bad arg, do perror("") or fprintf(stderr, ""); return;
use perror for system call errors and fprintf for user input errors


assignment 1 double quote segment
	'abc"   "def"   "ghi' = 1 arg, there is no double quote. It just looks for the next "
	remember to remove the quotes, try to make sure to use O(n)
		using destination and source pointers, squish
			source sees double quote and moves on
			destination copies over source char on each loop
		count and malloc, then squish as you move through


[[Basic UNIX]]
[[buffering]]
[[programs vs process]]
[[unix id's]]
[[signals]]
