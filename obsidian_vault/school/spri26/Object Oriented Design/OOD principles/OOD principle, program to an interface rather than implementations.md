
title

promotes behavioral consistency in that subclasses must behaves in the same way as the parent, not just share the same structure

subclasses should not have more strict validation on input parameters than the parent, must be at least as strict on output parameters

clarifies that inheritance should only be used when a subclass truly is-a substitute for the base class

