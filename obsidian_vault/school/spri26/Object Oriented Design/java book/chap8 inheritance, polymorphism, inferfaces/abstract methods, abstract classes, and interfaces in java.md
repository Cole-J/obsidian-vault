
the 'abstract' keyword in a base class or specific method tells the compiler that the content of that declaration will be filled in later by a subclass



interfaces are declared with the 'interface' keyword rather than the class keyword, and are completely abstract classes with abstract variables and methods

all content in the interface will need to be defined in a subclass

