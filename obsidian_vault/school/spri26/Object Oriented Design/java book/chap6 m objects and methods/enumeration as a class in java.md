
enums are defined within classes as

enum name {ENUM0, ENUM1, ENUM2}


enhanced enum

enum name {ENUM0(data), ENUM1(data), ENUM2(data), private final TYPE variable, private constructor(param) {code body}, public method(param){code body}}