
remember that
	classes are a blueprint that defines variables (state) and methods (behavior)
	-
	objects are a specific instance created from a class that occupies memory and has a unique state

[[the Object class in java]]

toString()

equals(Object obj)

hashCode()

getClass()

clone()

wait(), notify(), notifyAll()

finalize()