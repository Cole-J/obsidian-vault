
static methods can only read or write to static fields

non static fields can read or write to anything (static or not)

static methods do not have an implicit this. (remember this. is to call back to the class)

