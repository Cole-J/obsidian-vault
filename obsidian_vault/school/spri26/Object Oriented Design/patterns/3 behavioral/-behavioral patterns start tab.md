
[[OOD pattern 1 iterator]]
	The iterator pattern defines a way to move through a data structure encapsulated away from the client.

[[OOD pattern 2 listener]]
	was given for the class, but seems to just be a mutated observer.

[[OOD pattern 3 observer]]
	The observer pattern defines a way to implement a call / respond, notify, or subscription style communication between objects.