
sudo (builtin, admin with access to root, with quickhand root access)

media-full-access (able to access media drives)
media-pnas-access
media-rnas-access
media-playground-access
media-

srvsys-newuser-access (can create a new user)
	make a script that creates a new user, and only this can access it
	under /srvsys/newuser
srvsys-info-access (can view server log and info files saved in /srvsys/info)