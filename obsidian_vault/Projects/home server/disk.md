
main 500gb SSD
	OS
	cli users (users should move to playground for large tasks)
		colejadmin (users home '$:' is for documentation)
		colej
	programs and program management
		docker
	server infrastructure (as code, IaC)

fast (storage) 500gb SSD
	users fast storage
		fast sync
			obsidian
		fast storage
	staging
		(auto delete contents if not used for 1w)

playground 250gb SSD
	public
	cli users

public 1tb HDD

restricted 8tb HDD
	sync
	nas
		users
		software
		media

cache 256gb nvme m.2
	set as cache for the 8tb HDD