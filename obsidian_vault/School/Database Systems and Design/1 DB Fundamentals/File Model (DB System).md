
The File System model is similar (or is) to storing information in file directories and subdirectories. This can work with small amounts of data, or data designed to work in a file system (computer programs). 

However it struggles with large interconnected data. It becomes more complex over time and data is isolated in its own directory making searches difficult.