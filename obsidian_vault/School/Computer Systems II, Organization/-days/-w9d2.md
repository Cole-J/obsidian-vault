
use condition variables / monitor for a5
concurrent euclid

check slides

asked to only have 1 file

looking for
	thread body func
		//does not do sync
		get craft
			early exit if line too long
		use craft
		return craft
		// get and return should be the ones that mess with cond and mutex

waiting on condvar
	queue has condition variable
	maybe also have num of jackets needed
	if you need to wait
		get a queue element malloc
		prepare condition variable
		add to end of queue
		wait on condition variable
		now head of queue
			dequeue
			destroy condition variable
			free node
	get craft and return craft are monitor functions
	critical (locked)
		queue modifications
		life jacket modifications

condition variable
	allows you to block code and have someone else tell to start again
	pthread_cond_signal or broadcast(condition variable)

change queue to be
	queue element
		life jackets needed
		threads condition variable

make sure to create random waits each loop of the main loop