output containing all pairs of tuples from two different relations

num of attributes of (r x s) = num of attributes of r + num of all attributes of s

cartesian product of two tables with common attributes
	it will create a table with the sum of attributes
	if B is the same attribute between relation r and s
	there will be a rB and a sB attribute