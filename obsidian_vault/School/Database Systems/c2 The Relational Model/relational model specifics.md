Attributes (columns) types
	Domain of the attribute
		set of allowed values for an attribute
			ex
				domain of salary attribute is the set of all possible salary values
	Attributes are normally atomic (indivisible) 
		[[atomic vs non atomic]]