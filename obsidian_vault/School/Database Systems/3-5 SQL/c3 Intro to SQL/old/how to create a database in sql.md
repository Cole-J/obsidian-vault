
which part of SQL is used to create a DB
	the DDL


data types of attributes supported in sql
	char(n) or character(n)
		fixed length string with specified length n
	varchar(n)

when creating a DB with multiple tables
	first create the table with no connections to others (foreign keys)

create database command
use command to select DB
create table