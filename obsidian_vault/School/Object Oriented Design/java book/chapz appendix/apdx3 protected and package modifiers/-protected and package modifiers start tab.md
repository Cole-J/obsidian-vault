
protected class modifier 

the protected object cannot be called in any derived class, but not outside

if v is a protected variable in class A and class B is derived from A
	v is public to classes A and B
	v is private to any class not derived from A

skipping over packages
