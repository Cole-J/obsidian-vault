
thread attributes

reentrancy, thread safe

thread specific data
	global variable in the thread, but only in the thread (local to the thread itself)
	keys
		thread specific, use a threads key instead of thread id