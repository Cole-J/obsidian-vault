
what is an OS
	service provider to 'user' (programs running on the OS)
		execute programs with thread control
		input/output
			local machine
			network
		memory managment
		file managment
		
	may not provide user interface
		X windows is not a part of the OS
		mac OS X user interface is also not a part of the OS
		shell / cmd is not a part of the OS
			first 4 assignments are making your own UI for the OS systems
			shells are interactive and can run commands
			shell scripts
			some are built into (buildin type) the shell, while others are programs
	for win 32, there was no UI within OS
		later moved so that the UI is a part of the OS to increase speed