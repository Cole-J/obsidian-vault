
a3 command expansion $(...)
	./ush script $(ls) -> ./ush script file1 file2 file3 ...

ush main -> processline -> argparse -> exec

in expand find $(, save pointer to the start of the command after $(, find matching ), there can be nested (...(...)), find proper ), int to track starts at 1, stops when it goes to 0
	-
	replace ) with EOS
	-
	create a pipe / check for errors
	process the line with the write end of the pipe as stdout
		add args to process to make processline(outfd, wait/dont flags)
		processline() must not wait for child to finish, just return
		parent / child can handle 64k, check if it needs to wait before waiting
		processline should return the pid of the child started or an error value
	close write end of pipe so next loop will get EOF
	(all in expand)
	Real LOOP
		best to read directly into the new expanded string
		do not read 1 char at a time
		keep reading until EOF (success) or buffer is full (error)
	AFTER completing the read, if the last character read is \n, remove it
	all other \n should be made spaces
	cleanup, close read end of pipe, wait for child if one started in processline
		do not have to report on the child
		remember the value for $?
	update sstat
		man dprintf

in expand
	pid = processline(..., pipeWriteEnd, NOWAIT)

200,000 used in processline, can be passed to expand but never created in expand

procesline changes
	int processline(char \*line, int outfd, int flags)
	change to main, new call is processline(buffer, 1, WAIT);
		output should go to stdout and wait for child
	change to processline
		in child only, if outfd != 1, put the outfd on 1
			man dup2
			dup2(outfd, 1)
			no dup2 in parent
		in parent only, should we wait or not
				\#define WAIT 1 // in defn.h
				\#define NOWAIT 0 // in defn.h
			if wait, do standard stuff and wait and process $? and report signals
			if not just return the pid of the child started
			if statement
				if (flags&WAIT) {do something}
			if builtin commands are called, hand bi the outfd
			sstat is the only one that printf to stdout

processing order
	main, remove comments
	processline
		expansion, in expand
		pipeline identification, in processline
			on each element of the pipeline / only element
				arg parsing
				execution
					builtin or fork/exec, run it
					

do before pipeline
[[implementing pipelines]]



