
a3
	processline(line, infd, outfd, flags)
		expand line or copy
		child
			dup2 to work with pipes
				dup2 for stdout
				dup2 for stdin (only place for dup2 stdin)
				when we exec 0 and 1 should be what we want to read and write from
		refactor processline to work with that
		main will call main(buffer, 0 stdin, 1 stdout, flags)
		-
		pipeline section
		if pipeline
			process pipes
		else
			base case, command, fork exec or bi
	expand()
		processline(subcommand, 0, write end of pipe, flags)
		read from pipe in expand
	-
	infd -> a | b | c -> outfd
		no more expand left, check for pipes after expand
		need a zombie killer at start of processline, wait with WNOHANG
		function zombiekiller
		explicitly wait on c or final

[[signals basics (expanded) (chapter 10)]]
[[advanced signal interface]]

[[process environment (chapter 7)]]
