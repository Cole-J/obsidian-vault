
looking into class / interface
03_UML_Class_Diagram.docx, inheritance vs implementation
class lesson plans are in files

extending cpp classes
diamond problem
multiple inheritance


programming to an interface discussed in OOD principles docx in canvas files

assignment part c
	when iterating through the backpack keep track of current pocket as well
	call next() and hasnext() from list
	still have to make iterable iterator in pocket, delegate to the list
	iterable interface, return list.iterator
	iterator interface, has next on the list, next of the list
	-
	DO NOT store the pockets to a list in the backpack
	have them as fields, main pocket field, 

TEST FRIDAY