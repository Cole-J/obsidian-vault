quiz stuff,
	if given a cpp or java code section, is an object allocated to the stack or heap
	if its a pointer to a new object, it goes to the heap

reference variable = pointer, same thing

little vs big endian
	little endian 50 00 00 00 = 80 as 0x50 = 80
	the same in big endian i 00 00 00 50
	in VS memory window, the information in each address is in hex and little endian

```
Thermometer* thermometer = new Thermometer(80)
```
thermometer is on the stack, but the 80 is on the heap

should finish reading the memory management reading in canvas

make sure to set dangling pointers to nullptr directly after freeing the referenced object from memory

if you have multiple pointers pointing to the same object, make sure you only call delete/free on ONE pointer. trying to delete the same object multiple times from different pointers will cause issues.