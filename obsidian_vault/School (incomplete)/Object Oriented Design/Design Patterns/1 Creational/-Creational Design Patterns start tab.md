
The

The [[Design Pattern 9, Singleton|Singleton Pattern]] defines a way to ensure that a class is only one of itself created, essentially creating a global class. Or in other words, the entire class is static and only ONE can exist. It can also only be access by specific methods defined in its class.